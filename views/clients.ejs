<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clients</title>
</head>
<body>
    <h1>Clients</h1>
     <% if (locals.clients) { %>
     <% clients.forEach((client,i) => { %>
     name :  <%= client.name %> <br>
      field :<%= client.field %> <br>
      email :<%= client.email %> <br><br>
     <% }) %>
    <% } %>
</body>
</html>